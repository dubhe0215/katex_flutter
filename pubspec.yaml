name: katex_flutter
description: Render scientific equotions using the KaTeX library
version: 0.0.1
homepage: https://gitlab.com/testapp-system-katex_flutter

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: '^0.3.19+5'

dev_dependencies:
  flutter_test:
    sdk: flutter
